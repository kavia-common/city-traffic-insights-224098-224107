# Traffic Backend - Example Environment Variables
# Copy this file to .env and fill in the values. Do NOT commit your real .env.

# -----------------------------------------------------------------------------
# Core runtime
# -----------------------------------------------------------------------------
# Node environment (development | production | test)
NODE_ENV=development

# Backend HTTP port (defaults to 3001 if unset)
PORT=3001

# Host binding (optional, defaults to 0.0.0.0)
# HOST=0.0.0.0

# -----------------------------------------------------------------------------
# Frontend origin and API base
# -----------------------------------------------------------------------------
# Used by CORS to allow the browser client. Set to your frontend URL.
# Example local dev: http://localhost:3000
# For permissive mode (not recommended for prod), you can use '*'
REACT_APP_FRONTEND_URL=http://localhost:3000

# Optional: computed API base that the frontend may use; not required by backend
# REACT_APP_API_BASE=http://localhost:3001
# REACT_APP_BACKEND_URL=http://localhost:3001

# Optional: WebSocket base if you add websockets later
# REACT_APP_WS_URL=ws://localhost:3001

# -----------------------------------------------------------------------------
# Logging and server behavior
# -----------------------------------------------------------------------------
# Log level for winston (error | warn | info | http | verbose | debug | silly)
REACT_APP_LOG_LEVEL=debug

# Trust proxy (true when behind a reverse proxy / load balancer)
REACT_APP_TRUST_PROXY=true

# Healthcheck path
REACT_APP_HEALTHCHECK_PATH=/api/health

# -----------------------------------------------------------------------------
# Features / flags (optional)
# -----------------------------------------------------------------------------
REACT_APP_FEATURE_FLAGS=
REACT_APP_EXPERIMENTS_ENABLED=false

# -----------------------------------------------------------------------------
# MongoDB (optional but recommended for history persistence)
# -----------------------------------------------------------------------------
# Atlas example:
# MONGO_URI=mongodb+srv://<USER>:<PASSWORD>@<CLUSTER>.mongodb.net/traffic?retryWrites=true&w=majority
# Local example:
# MONGO_URI=mongodb://localhost:27017/traffic
MONGO_URI=

# -----------------------------------------------------------------------------
# TomTom (optional: enables real traffic for /api/traffic/live)
# -----------------------------------------------------------------------------
# Get a key from https://developer.tomtom.com/ and enable Traffic Flow API access
TOMTOM_API_KEY=

# -----------------------------------------------------------------------------
# Misc (frontend build variables present in container but not required by backend)
# -----------------------------------------------------------------------------
REACT_APP_NODE_ENV=development
REACT_APP_ENABLE_SOURCE_MAPS=true
REACT_APP_PORT=3001
